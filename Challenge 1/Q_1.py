import numpy as np
import math
import matplotlib
import matplotlib.pyplot as plt

G = 6.67 * 10 ^-11

#Orbital position of mercury will be x-y plot only, this assumes that we will only
#be tracking x and y movements, and no z axis is considered.

x_vals = []
y_vals=

def Euler(method):
    """
    Function for Euler's method with standardised input and outputs'
    """
    x_n = x_n-1 + h